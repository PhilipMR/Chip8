{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run target",
            "type": "cppvsdbg",
            "request": "launch",
            "preLaunchTask": "build",
            "program": "${command:cmake.launchTargetPath}",
            "cwd": "${workspaceFolder}/build"
         }
    ]
}